receivers:
  otlp:
    protocols:
      http:
        endpoint: "localhost:4318"

exporters:
  awsxray:
    region: "us-west-2"

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [awsxray]
