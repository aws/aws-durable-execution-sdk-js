.idea
/node_modules
**/node_modules 
/build
**/build
/dist
*/dist
**/dist
**/dist-cjs
/coverage
**/coverage
*.iml
.aws-sam/*
*.zip
.DS_Store
.eslintcache
*.tsbuildinfo
.rollup.cache
**/.rollup.cache
*.d.ts.map
*.d.ts
*.tgz

# VS Code
.vscode/

# Environment files
.secrets
.env
.env.local

# Node version
.nvmrc
mise.toml

*.rollup.cache

# Durable executions local emulator files
# All emulator output, not just its SDK cache.
**/.durable-executions-local/
# SDK reflection cache written at the repo root. Same reason as the webview bundles
# below: one shared working tree, so this must be ignored on every branch.
/.aws-sdk-cache/
# Built webview bundles (webview-ui's esbuild writes these, including a ~270k-line
# Monaco worker). Ignored here rather than only alongside the code that produces them,
# because the monorepo shares one working tree: without it, a `git add -A` on ANY
# branch sweeps them in.
packages/aws-durable-execution-sdk-js-insight-vscode/media/

# CDK synth/deploy output
**/cdk.out/
packages/aws-durable-execution-sdk-js-cdk/deploy-verify/
# Deploy outputs — contain real account ids and resource ARNs
**/cdk-outputs.json
